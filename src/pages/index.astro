---
import { getPosts } from "@/posts"
import BlogPostList from "@/layouts/BlogPostList.astro";

const posts = await getPosts()
    .then((posts) => posts.slice(0, 15));
---

<BlogPostList title="" posts={posts} />
