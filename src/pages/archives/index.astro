---
import BlogPostList from "../../layouts/BlogPostList.astro";

const posts = Object.values(import.meta.glob('../../archives/*.md', { eager: true })).reverse();
---

<BlogPostList title="Archives" posts={posts} />
